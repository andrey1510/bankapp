server:
  port: 8884

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8885/eureka/