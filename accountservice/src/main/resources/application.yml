server:
  port: 8881

spring:
  application:
    name: accountservice
  datasource:
    url: jdbc:postgresql://localhost:5554/accountdb
    username: login
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect