server:
  port: 8890

spring:
  application:
    name: notificationservice
  datasource:
    url: jdbc:postgresql://notificationdb:5432/notificationdb
    username: login
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/banksapp

  mail:
    host: mailhog
    port: 1025
    username:
    password: