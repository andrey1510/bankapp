server:
  port: 8887

spring:
  application:
    name: exchangeservice
  datasource:
    url: jdbc:postgresql://localhost:5558/exchangedb
    username: login
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8892/realms/banksapp