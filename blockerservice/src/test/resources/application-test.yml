server:
  port: 8882

spring:
  application:
    name: blockerservice
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8892/realms/banksapp

eureka:
  client:
    enabled: false
